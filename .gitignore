# CMake build output
/build/

# Publish target written by scripts/build.sh: generated vddsound.dll, copies of
# redeploy.bat/install.reg, plus local DOS test binaries (MIDIPLAY.EXE, *.mid)
# and trace.log. All artifacts / third-party — kept local, not versioned.
/vddsound/

# Microsoft NTVDM binaries used locally for reverse-engineering offsets.
# COPYRIGHT — must NOT be committed/pushed. Supply your own XP SP3 ntvdm.exe.
/vdm/

# Logs
*.log
trace.log

# macOS / local tooling
.DS_Store
.claude/settings.local.json
